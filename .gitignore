secrets.generated.txt
.env
.env.*
!.env.example
node_modules/
.DS_Store
.cursor/
.idea/
.vscode/

# Root-level generated/temp files
make_logo.py
manicbot_logo.png
.cursor-project-analysis-prompt.txt

# Generated static blog output (standalone `node generate.mjs` in manicbot-blog)
manicbot-blog/dist/
# wrangler local state
.wrangler
.dev.vars*
!.dev.vars.example

# Claude Code ephemeral worktrees (each is its own git repo — never commit as submodules)
.claude/worktrees/
.claude/settings.local.json

# IG/Meta tokens — never commit plaintext tokens
*.token
*-token-*.txt
ig-direct-token*
fb-page-token*
**/.config/manicbot/**

# Seed scripts with plaintext credentials — kept on developer machine only.
# A `.example` template (with `process.env.SEED_*_PASSWORD` placeholders) lives
# in the repo so anyone can reconstruct the structure without learning a real
# password. See manicbot/scripts/seed-web-users.example.mjs.
manicbot/scripts/seed-web-users.mjs
manicbot/scripts/seed-*-users.mjs
!manicbot/scripts/seed-*-users.example.mjs

# 2026-05-27 — Internal-only docs kept off the public repo. Working
# copies live in _docs-local/ on each developer's machine. If you need
# context that one of these docs would provide, ping the maintainer.
_docs-local/
CLAUDE.md
TEST_ACCOUNTS.md
CALENDAR_OVERHAUL_PLAN.md
manicbot/SECURITY_FINDINGS.md
manicbot/ARCHITECTURE.md
manicbot/SECRET_ROTATION.md
manicbot/LANDING_DEMO_INTEGRATION.md
manicbot/docs/oauth-verification-kit.md
manicbot/docs/marketing/BRAND_VOICE.md
docs/pre-launch-blockers-report.md

# 2026-05-28 — Local working notes that previously sat untracked in root.
# Belong on developer's machine only (audit reports, weekly routine drafts,
# overview docs). The public repo flipped on 2026-05-27 (commit 1f521ab) and
# these files are explicitly NOT for distribution.
/Ревью*/
/.claude/weekly-routines-drafts/
/.claude/scheduled_tasks.lock
/CLAUDE_CODE_MULTI_AGENT_AUDIT_PROMPT.md
/PROJECT_OVERVIEW_RU.md

# 2026-06-06 — ThinkPad automation scripts (local source of truth, deployed via rsync).
# Contains cron scripts, agents, and .env.example — never committed.
_automation/

# 2026-06-07 — ThinkPad remote-ops scripts that get dropped in the repo ROOT
# (LOCAL-ONLY, never commit — the public repo would leak them).
#   bot_remote.js       — Telegram→shell ops bot; hardcoded TELEGRAM_TOKEN + GROQ_KEY
#   ecosystem_remote.js — PM2 config snapshot for the ThinkPad
#   machine_remote.md   — host/data layout notes
#   run_all_crons.sh    — PM2 restart helper
# Canonical source of truth lives in _automation/. Secrets belong in env, not in code.
/bot_remote.js
/ecosystem_remote.js
/machine_remote.md
/run_all_crons.sh
